import { Button } from '@pancakeswap/uikit'

export const SwapSelection = () => {
  return (
    <Button
      mb={3}
      scale="sm"
      fullWidth
      variant="subtle"
      disabled
    >
      SWAP
    </Button>
  )
}
